## Agent skills

### Issue tracker

GitHub Issues (MasterATM/amigoSecreto), no PR triage. See `docs/agents/issue-tracker.md`.

### Triage labels

Default labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context. `CONTEXT.md` at root, `docs/adr/` at root. See `docs/agents/domain.md`.
